
  # Payroll System UI UX Design

  This is a code bundle for MICROPAY Payroll System UI UX Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
